apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
	compileOptions.encoding = "UTF-8"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 21
    }
}

dependencies {
    compile project(':zhangyf.vir56k.pulltorefresh')
}
